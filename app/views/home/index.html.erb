<!DOCTYPE html>
<html>
<head>
  <title>Index</title>
</head>
<body>

<% @sessions.each do |session| %>
  <tr>
    <% if session.user_id == current_user.id %>
      <td><%= session.user_id%></td>
      <td><%= session.name %></td>
    <%end %>
  </tr>
<% end %>


</body>
</html>